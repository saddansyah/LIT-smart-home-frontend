import { createStore } from 'vuex'

import device from './device.store'

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    device
  }
});